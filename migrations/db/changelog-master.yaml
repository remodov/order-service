# Корневой Liquibase changelog. Версионируется по релизам: v-1.0/, v-1.1/, …
# Никогда не редактируй уже применённый changeSet — добавляй новый файл и include.
databaseChangeLog:
  - include: { file: db/changelog/v-1.0/00-enum-types.yaml }
  - include: { file: db/changelog/v-1.0/01-initial-schema.yaml }
